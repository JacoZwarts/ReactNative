import React, { useState } from "react";
import { View, Button, StyleSheet } from "react-native";

const ColourScreen = ({navigation}) => {
  return (
    <View>
      <Button title="Test"></Button>
    </View>
  );
};

const styles = StyleSheet.create({});

export default ColourScreen;
